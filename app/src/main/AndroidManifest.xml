<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rizkykhapidsyah">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.timePicker2"></activity>
        <activity android:name=".activity.timePicker" />
        <activity android:name=".activity.spinnerView" />
        <activity android:name=".activity.menuLayout" />
        <activity android:name=".activity.imageSwitcher" />
        <activity android:name=".activity.galleryView2" />
        <activity android:name=".activity.galleryView" />
        <activity android:name=".activity.datePicker2" />
        <activity android:name=".activity.datePicker" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>